
import numpy as np
cimport numpy as np






def entropy(Y):
	"""
	Returns the entropy as defined in information theory
	"""
	entropy = 0
	for val in list(set(Y)):
		p = (Y==val).sum()*1.0 / len(Y)
		entropy -= p*np.log2(p)
	return entropy

